version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions

  # npm ecosystem activates after package.json is committed (Phase 0.1)
  # - package-ecosystem: npm
  #   directory: /
  #   schedule:
  #     interval: weekly
  #   open-pull-requests-limit: 10
  #   labels:
  #     - dependencies
  #     - npm
